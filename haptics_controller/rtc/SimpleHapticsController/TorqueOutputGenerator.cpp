#include "TorqueOutputGenerator.h"

// output Torqueを計算する
bool TorqueOutputGenerator::calcTorque(const GaitParam& gaitParam,// input
                                       cnoid::BodyPtr& actRobotTqc) const{ // output

  return true;
}
