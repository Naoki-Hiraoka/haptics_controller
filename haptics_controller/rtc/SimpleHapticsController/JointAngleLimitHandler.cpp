#include "JointAngleLimitHandler.h"

// jalhTorqueを計算する
bool JointAngleLimitHandler::calcTorque(const GaitParam& gaitParam, double dt,// input
                                        cnoid::VectorX& o_jalhTorque) const{ // output

  return true;
}
