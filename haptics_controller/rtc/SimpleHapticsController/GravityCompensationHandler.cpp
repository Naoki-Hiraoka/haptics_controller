#include "GravityCompensationHandler.h"

// gchTorqueを計算する
bool GravityCompensationHandler::calcTorque(const GaitParam& gaitParam, double dt,// input
                                            cnoid::BodyPtr& actRobotTqc, cnoid::VectorX& o_gchTorque) const{ // output

  return true;
}
